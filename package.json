{
  "name": "hive-auditor-v1-2",
  "version": "1.2.0",
  "private": true,
  "description": "Independent live Bot Council auditor with learning, verified findings, and copy-ready update prompt generation.",
  "scripts": {
    "start": "node server.js",
    "test": "node server.js --self-test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "adm-zip": "^0.5.16",
    "typescript": "^5.8.0"
  }
}
